# ActiveLearningBirds

The main loop for active learning  is found in the training_loop.py script

There are several other scripts used to create the dataset splits and to create the feature extractions from the pretrained CNN network.
